# إعداد Railway - خطوات مفصلة

## المشكلة الحالية
```
Error: the URL must start with the protocol `postgresql://` or `postgres://`
```

هذا يعني أن `DATABASE_URL` غير موجود أو قيمته خاطئة في Railway.

## الحل خطوة بخطوة:

### 1. اذهب إلى Railway Dashboard
- افتح مشروعك على [railway.app](https://railway.app)
- اضغط على المشروع

### 2. افتح Variables (متغيرات البيئة)
- من القائمة الجانبية، اضغط على **"Variables"**
- أو من Settings → Variables

### 3. أضف `DATABASE_URL`
- اضغط على **"+ New Variable"**
- **Name:** `DATABASE_URL`
- **Value:** 
  ```
  postgresql://neondb_owner:npg_c76Mhunmfgbd@ep-young-flower-a1wp3ydx-pooler.ap-southeast-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
  ```
- ⚠️ **مهم جداً:** 
  - تأكد من نسخ القيمة كاملة بدون مسافات
  - تأكد من أنها تبدأ بـ `postgresql://`
  - لا تضيف علامات اقتباس حول القيمة

### 4. (اختياري) أضف `NODE_ENV`
- **Name:** `NODE_ENV`
- **Value:** `production`

### 5. احفظ التغييرات
- اضغط على **"Save"** أو **"Add"**
- Railway سيعيد تشغيل التطبيق تلقائياً

### 6. تحقق من النتيجة
- اذهب إلى **"Deployments"** → **"View Logs"**
- يجب أن ترى:
  ```
  ✅ Database connection established
  🚀 Application is running on: http://0.0.0.0:PORT
  ```

## التحقق من القيمة:

بعد إضافة `DATABASE_URL`، تأكد من:
1. القيمة تبدأ بـ `postgresql://` (وليس `postgres://` فقط)
2. لا توجد مسافات في البداية أو النهاية
3. القيمة كاملة ومطابقة للقيمة في `.env.production`

## إذا استمرت المشكلة:

1. **احذف المتغير وأضفه مرة أخرى** - قد يكون هناك مسافات خفية
2. **تحقق من Logs** - ابحث عن رسائل الخطأ الجديدة
3. **تأكد من أن قاعدة البيانات متاحة** - جرّب الاتصال من خارج Railway

