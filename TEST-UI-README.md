# 🧪 دليل اختبار الواجهات الأمامية - IBEX

## نظرة عامة

تم إنشاء صفحة HTML مؤقتة وسكربتات لتشغيل الفرونت إند بسهولة من أجل الاختبار المرئي للواجهات.

## الملفات المُنشأة

1. **`apps/frontend/test-index.html`** - صفحة HTML مؤقتة تعرض قائمة بجميع الواجهات
2. **`start-frontend-test.ps1`** - سكربت PowerShell لتشغيل الفرونت إند
3. **`start-frontend-test.bat`** - سكربت Batch لتشغيل الفرونت إند

## طريقة الاستخدام

### الطريقة 1: استخدام سكربت PowerShell (موصى به)

```powershell
.\start-frontend-test.ps1
```

### الطريقة 2: استخدام سكربت Batch

```cmd
start-frontend-test.bat
```

### الطريقة 3: التشغيل اليدوي

1. افتح Terminal في مجلد المشروع
2. انتقل إلى مجلد الفرونت إند:
   ```bash
   cd apps/frontend
   ```
3. شغّل خادم التطوير:
   ```bash
   npm run dev
   ```
4. افتح ملف `test-index.html` في المتصفح:
   - انقر نقراً مزدوجاً على الملف، أو
   - افتحه من المتصفح: `file:///path/to/apps/frontend/test-index.html`

## الواجهات المتاحة

### ✅ الواجهات المكتملة (Stage 1-3.2)

- **لوحة التحكم** (`/`) - الصفحة الرئيسية
- **قائمة الفواتير** (`/invoices`) - مع تصميم محسّن (Stage 3.1)
- **تفاصيل الفاتورة** (`/invoices/[id]`) - مع تصميم محسّن (Stage 3.2)

### 📝 الواجهات الموجودة (قبل التحسين)

- **فاتورة جديدة** (`/invoices/new`)
- **تعديل الفاتورة** (`/invoices/[id]/edit`)
- **إصدار الفاتورة** (`/invoices/[id]/issue`)
- **السجل المالي** (`/ledger`)

### 🔮 الواجهات المستقبلية

- **التقارير** (`/reports`) - قريباً
- **الاشتراكات** (`/billing`) - قريباً

## ملاحظات مهمة

1. **الصلاحيات**: جميع الواجهات تتطلب صلاحيات معينة. تأكد من أن المستخدم التجريبي لديه الصلاحيات المناسبة.

2. **البيانات**: بعض الواجهات تحتاج إلى بيانات موجودة (مثل ID الفاتورة). استخدم البيانات التجريبية المتاحة.

3. **الخادم**: تأكد من تشغيل خادم التطوير (`npm run dev`) قبل الوصول إلى الواجهات.

4. **صفحة الاختبار**: صفحة `test-index.html` هي صفحة مؤقتة للاختبار فقط وليست جزءاً من التطبيق النهائي.

## استكشاف الأخطاء

### المشكلة: لا يمكن الوصول إلى الواجهات

**الحل**: تأكد من:
- تشغيل خادم التطوير (`npm run dev`)
- أن الخادم يعمل على `http://localhost:3000`
- أن المتصفح لا يحجب الروابط

### المشكلة: صفحة الاختبار لا تفتح

**الحل**: افتح الملف يدوياً من المتصفح:
```
file:///D:/ibex25/apps/frontend/test-index.html
```
(استبدل المسار بمسار المشروع الخاص بك)

### المشكلة: الواجهات تظهر فارغة

**الحل**: 
- تحقق من وجود بيانات في قاعدة البيانات
- تحقق من الصلاحيات
- تحقق من console المتصفح للأخطاء

## التطوير المستقبلي

عند إضافة واجهات جديدة، قم بتحديث ملف `test-index.html` لإضافتها إلى القائمة.

---

**آخر تحديث**: بعد Stage 3.2

